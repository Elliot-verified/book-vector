# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Node / web
node_modules/
web/dist/
.vercel/

# Environment / secrets
.env
.env.*
!.env.example

# Data & generated artifacts (see PLAN.md — vendored/derived, not committed)
pipeline/data/*
!pipeline/data/.gitkeep
web/public/data/*
!web/public/data/.gitkeep
*.sqlite
*.sqlite3
*.tar.gz

# OS
.DS_Store

# Curated (not derived) — keep the hand-labeled eval set
!pipeline/data/eval_pairs.jsonl

# Deploy artifacts — committed so Vercel git builds have them (see PLAN.md).
# galaxy.json = client galaxy; web/data/vectors.* = the query fn's int8 vectors
# (web/data/ is not under the ignored web/public/data/, so it tracks normally).
!web/public/data/galaxy.json
